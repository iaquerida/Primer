// fragment not legal, lacks semicolon + the the << the left hand operator is empty in the next statements and separated with semicolons

/* Given:
std::cout << "The Sum of " << v1;
		  << " and " << v2;
		  << " is " << v1 + v2
		  << std::endl;

*/
